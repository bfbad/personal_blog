<!DOCTYPE html>
<html>
<head>
  <title>Brandon Ferguson's Blog</title>
  <%= csrf_meta_tags %>

  <!-- Link Bootstrap -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
  <link href="https://fonts.googleapis.com/css?family=Lato|Open+Sans" rel="stylesheet">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= favicon_link_tag 'favicon.ico' %>
</head>

<body>
 	<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "Brandon Ferguson's Blog", root_path, class: "navbar-brand active nav-link" %>
     <!--  <a class="navbar-brand active" href="#">Blog</a> -->
      </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav pull-right">
          <li><%= link_to "About Me", pages_about_path, class: "nav-link" %></li>
          <li><%= link_to "Contact", pages_contact_path, class: "nav-link" %></li> 
          <li><% if user_signed_in? %>
                <%= link_to('My Account', edit_user_registration_path, class: "nav-link") %>
              <% end %>
          <li>
            <% if user_signed_in? %>
              <%= link_to('Logout', destroy_user_session_path, :method => :delete, class: "nav-link") %>
            <% else %>
              <%= link_to('Login', new_user_session_path, class: "nav-link") %>
            <% end %>
          </li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
  <% if notice %>
    <p class="notice alert alert-warning", role="alert"><%= notice %></p>
  <% end %>
  <% if alert %>
    <p class="alert alert-danger", role="alert"><%= alert %></p>
  <% end %>
  <div class="page-content">
    <%= yield %>
  </div>
</body>
</html>
